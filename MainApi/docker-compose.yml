services:
  mainapi:
    image: mainapi:latest
    build:
      context: ..
      dockerfile: MainApi/Dockerfile
    container_name: mainapi
    restart: unless-stopped
    ports:
      - "98:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://0.0.0.0:8080
