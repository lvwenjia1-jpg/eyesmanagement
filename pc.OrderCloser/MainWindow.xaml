<Window x:Class="OrderCloser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="订单关闭工具"
        Width="760"
        Height="620"
        MinWidth="640"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="查询并关闭订单" FontSize="22" FontWeight="SemiBold" Foreground="#0F172A" />
            <TextBlock Margin="0,6,0,0" Text="输入原始 订单号，工具会先查询交易并关闭全部原始 订单号。" Foreground="#64748B" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtTradeId" Height="38" Padding="12,8" VerticalContentAlignment="Center" FontSize="14" />
            <Button x:Name="BtnQueryAndClose" Grid.Column="1" Margin="12,0,0,0" Padding="18,8" Content="查询并关闭" Click="BtnQueryAndClose_Click" Background="#2563EB" Foreground="White" BorderBrush="#2563EB" />
        </Grid>

        <TextBox x:Name="TxtResult"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="12"
                 Padding="12"
                 Background="White"
                 BorderBrush="#CBD5E1"
                 BorderThickness="1" />

        <TextBlock x:Name="TxtStatus" Grid.Row="3" Margin="0,12,0,0" Foreground="#475569" Text="就绪" />
    </Grid>
</Window>
